# Port for your Netty web service
server:
  port: 9000

spring:
  rsocket:
    server:
      # RSocket server port
      port: 7000
      # RSocket transport protocol
      transport: tcp


logging:
  level:
    org:
      springframework:
        amqp: DEBUG
        messaging: DEBUG
        data: DEBUG
    com:
      greglturnquist:
        hackingspringboot: DEBUG
    reactor: DEBUG